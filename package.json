{
  "name": "bryanlee.love",
  "scripts": {
    "dev": "imba -w index.html",
    "build": "imba build index.html",
    "deploy": "npm run build && npx touch dist/public/.nojekyll && echo bryanlee.love > dist/public/CNAME && npx gh-pages --dotfiles --dist dist/public"
  },
  "devDependencies": {
    "imba": "^2.0.0-alpha.231"
  }
}
